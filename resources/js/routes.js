import VueRouter from "vue-router";

const routes = [
    // {
    //     path: '/filmes/:channel',
    //     component: Titles,
    //     name: 'frontend-movies'
    // },
];

const router = new VueRouter({
    mode: "history",
    routes
})

export default router;
